define 'PoliticsPanel', ['Panel', 'Text', 'Button'], (Panel, Text, Button) ->
  class PoliticsPanel extends Panel
    constructor: (@menu) ->
      @label = 'Politics'
      super @menu, @label
      return

    init: () ->
      super()



    draw: () ->
      super()
      return
